# Project Descriptor

```
<project descriptor> ::= {
   package_cache:
   package_materializations:
   dependency_configuration:
   dependencies: {  }
   dependency_dir:
   config_dir:
   project_file:
   package_descriptor_file:
}
```
