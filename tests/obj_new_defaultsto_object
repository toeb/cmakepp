function(test)
	obj_new(obj)
	assert(obj)
	obj_getprototype(${obj} proto)
	assert(proto)
	obj_istype(${obj} res Object)
	assert(res)
	set(res)
	obj_callmember(${obj} to_string res)
	message("${res}")
	assert(res)


endfunction()